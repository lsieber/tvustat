{
	"autoload" : {
		"psr-4" : {
			"tvustat\\" : [
				"src",
				"src/conn",
				"src/elmt",
				"src/inp",
				"src/outp",
				"src/outp/bl",
				"src/utils"
			],
			"config\\" : [
				"config",
				"config/db"
			],
			"test\\" : [
				"tests",
				"tests/elmt"
			]
		}
	},
	"name" : "tvustat\\tvustat",
	"description" : "This is a Project which aims to extend the existing Swiss Athletics Database for STV Clubs",
	"require" : {
		"php" : ">=7.2"
	}
}